<% if model.errors.any? %>
  <div class="bg-red-50 border border-red-200 text-red-800 rounded-xl p-3 text-sm space-y-0.5">
    <% model.errors.full_messages.each do |msg| %>
      <p><%= msg %></p>
    <% end %>
  </div>
<% end %>
