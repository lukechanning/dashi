<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Dashi" %></title>
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-warm-bg min-h-screen"
        data-controller="timezone"
        data-timezone-current-value="<%= current_user&.timezone.to_s %>">
    <nav class="bg-white/80 backdrop-blur-sm border-b border-stone-200">
      <div class="max-w-4xl mx-auto px-4 py-3 flex items-center justify-between">
        <%= link_to root_path, class: "flex items-center gap-2" do %>
          <%= render "shared/logo", size: :sm %>
          <span class="text-lg font-bold tracking-tight text-stone-800">Dashi</span>
        <% end %>

        <%# Desktop links (hidden on mobile — AppBar handles mobile nav) %>
        <div class="hidden sm:flex items-center gap-4 text-sm">
          <%= link_to "Today", root_path, class: "text-stone-500 hover:text-stone-800 transition-colors" %>
          <%= link_to "Projects", projects_path, class: "text-stone-500 hover:text-stone-800 transition-colors" %>
          <%= link_to "Goals", goals_path, class: "text-stone-500 hover:text-stone-800 transition-colors" %>
          <% if current_user&.admin? %>
            <%= link_to "Invitations", invitations_path, class: "text-stone-500 hover:text-stone-800 transition-colors" %>
          <% end %>
          <span class="text-stone-300">|</span>
          <%= link_to account_path, class: "flex items-center gap-2 hover:opacity-80 transition-opacity" do %>
            <%= user_avatar(current_user, size: :sm) %>
            <span class="text-stone-500"><%= current_user.name %></span>
          <% end %>
          <%= button_to "Sign out", session_path, method: :delete, class: "text-stone-400 hover:text-stone-600 cursor-pointer transition-colors" %>
        </div>
      </div>
    </nav>

    <%# Main content — extra bottom padding on mobile to clear the AppBar %>
    <main class="max-w-4xl mx-auto px-4 py-8 pb-28 sm:pb-8">
      <% if flash[:notice] %>
        <div class="bg-emerald-50 border border-emerald-200 text-emerald-800 rounded-lg p-3 mb-6 text-sm">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="bg-red-50 border border-red-200 text-red-800 rounded-lg p-3 mb-6 text-sm">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <%# Bottom AppBar (mobile + tablet) %>
    <% if current_user %>
      <div class="sm:hidden">
        <%= render "shared/app_bar" %>
      </div>

      <%# Floating action button (desktop only) %>
      <%= link_to new_todo_path, class: "hidden sm:flex items-center justify-center fixed bottom-6 right-6 w-14 h-14 bg-violet-600 hover:bg-violet-700 active:bg-violet-800 text-white rounded-full shadow-lg hover:shadow-xl transition-all duration-150 z-50", title: "New todo" do %>
        <svg xmlns="http://www.w3.org/2000/svg" class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
        </svg>
      <% end %>
    <% end %>
  </body>
</html>
