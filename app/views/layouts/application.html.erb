<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Dashi" %></title>
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-warm-bg min-h-screen"
        data-controller="timezone"
        data-timezone-current-value="<%= current_user&.timezone.to_s %>">
    <nav class="bg-white/80 backdrop-blur-sm border-b border-stone-200" data-controller="menu">
      <div class="max-w-4xl mx-auto px-4 py-3 flex items-center justify-between">
        <%= link_to root_path, class: "flex items-center gap-2" do %>
          <%= render "shared/logo", size: :sm %>
          <span class="text-lg font-bold tracking-tight text-stone-800">Dashi</span>
        <% end %>

        <%# Desktop links %>
        <div class="hidden sm:flex items-center gap-4 text-sm">
          <%= link_to "Today", root_path, class: "text-stone-500 hover:text-stone-800 transition-colors" %>
          <%= link_to "Projects", projects_path, class: "text-stone-500 hover:text-stone-800 transition-colors" %>
          <%= link_to "Goals", goals_path, class: "text-stone-500 hover:text-stone-800 transition-colors" %>
          <% if current_user.admin? %>
            <%= link_to "Invitations", invitations_path, class: "text-stone-500 hover:text-stone-800 transition-colors" %>
          <% end %>
          <span class="text-stone-300">|</span>
          <span class="text-stone-400"><%= current_user.name %></span>
          <%= button_to "Sign out", session_path, method: :delete, class: "text-stone-400 hover:text-stone-600 cursor-pointer transition-colors" %>
        </div>

        <%# Hamburger button (mobile only) %>
        <button class="sm:hidden p-1.5 rounded-md text-stone-500 hover:text-stone-800 hover:bg-stone-100 transition-colors"
                data-action="click->menu#toggle"
                aria-label="Toggle menu">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>

      <%# Mobile dropdown %>
      <div class="hidden sm:hidden border-t border-stone-100" data-menu-target="dropdown">
        <div class="max-w-4xl mx-auto px-4 py-3 flex flex-col gap-1 text-sm">
          <%= link_to "Today", root_path, class: "py-2 text-stone-600 hover:text-stone-900 transition-colors" %>
          <%= link_to "Projects", projects_path, class: "py-2 text-stone-600 hover:text-stone-900 transition-colors" %>
          <%= link_to "Goals", goals_path, class: "py-2 text-stone-600 hover:text-stone-900 transition-colors" %>
          <% if current_user.admin? %>
            <%= link_to "Invitations", invitations_path, class: "py-2 text-stone-600 hover:text-stone-900 transition-colors" %>
          <% end %>
          <div class="border-t border-stone-100 mt-1 pt-2 flex items-center justify-between">
            <span class="text-stone-400"><%= current_user.name %></span>
            <%= button_to "Sign out", session_path, method: :delete, class: "text-stone-400 hover:text-stone-600 cursor-pointer transition-colors" %>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-4xl mx-auto px-4 py-8">
      <% if flash[:notice] %>
        <div class="bg-emerald-50 border border-emerald-200 text-emerald-800 rounded-lg p-3 mb-6 text-sm">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="bg-red-50 border border-red-200 text-red-800 rounded-lg p-3 mb-6 text-sm">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
