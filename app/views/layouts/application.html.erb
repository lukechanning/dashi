<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Dashi" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-gray-50 min-h-screen">
    <nav class="bg-white border-b border-gray-200">
      <div class="max-w-4xl mx-auto px-4 py-3 flex items-center justify-between">
        <%= link_to "Dashi", root_path, class: "text-lg font-bold text-gray-900" %>
        <div class="flex items-center gap-4 text-sm">
          <%= link_to "Today", root_path, class: "text-gray-600 hover:text-gray-900" %>
          <%= link_to "Goals", goals_path, class: "text-gray-600 hover:text-gray-900" %>
          <%= link_to "Projects", projects_path, class: "text-gray-600 hover:text-gray-900" %>
          <span class="text-gray-400">|</span>
          <span class="text-gray-500"><%= current_user.name %></span>
          <%= button_to "Sign out", session_path, method: :delete, class: "text-gray-400 hover:text-gray-600 cursor-pointer" %>
        </div>
      </div>
    </nav>

    <main class="max-w-4xl mx-auto px-4 py-8">
      <% if flash[:notice] %>
        <div class="bg-green-50 border border-green-200 text-green-800 rounded-lg p-3 mb-6 text-sm">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="bg-red-50 border border-red-200 text-red-800 rounded-lg p-3 mb-6 text-sm">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
