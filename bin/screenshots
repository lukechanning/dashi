#!/usr/bin/env bash
set -e

mkdir -p screenshots

bundle exec rspec spec/system/screenshots_spec.rb --format documentation

echo ""
echo "Screenshots saved to screenshots/"
ls -1 screenshots/*.png 2>/dev/null || echo "(no PNGs found)"
